export const AppConfig = {
    serverUrl: null,   //Ip của service notification, userdata, trên môi trường uat sẽ để trống biến này. 
    maxContentSize: 100000,
    maxAdvertisement: 10,
    maxEffective: 30,
    maxCustodycd: 100,
}
